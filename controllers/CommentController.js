const { Comment } = require ('../db/schema')

const CreateComment = async ( req, resp ) => {
}

const DeleteComment = async ( req, resp ) => {

}

  module.exports = {
   CreateComment,
   DeleteComment
}
